# MapVideo ? ????????? ????????????? ???? ? MP4

???-?????? ??? ????????? ???????? ????????????? MP4-????? ? ?????? (???? ????? ??? ??????? ????? ????? ????????) ?? ?????? ?????? ?????? ????.

## ????????? ? ?????? ?? Windows (??? Docker)

### 1. ??????????????? ???????????

- Python 3.12+ ? ??????? ? python.org, ??? ????????? ???????? ??????? Add to PATH.
- FFmpeg ? ??????? ? gyan.dev/ffmpeg/builds (release full), ???????????, ???????? ????? bin ? PATH.
- Redis: ??? Windows ????????????? Memurai (memurai.com), ???? ??????? Redis ?? Linux-???????.
- Git ? ????? ?????????? ??????.

### 2. ???????????? ? ??????????? ?????????

    git clone <URL_REPO> maps
    cd maps
    python -m venv .venv
    .venv\Scripts\activate
    pip install -r backend\requirements.txt

### 3. ????????? .env

?????????? ??????:

    copy .env.example .env

?????????????? .env ? ??????? ???? ?????:

    YANDEX_API_KEY=<???? ??? ??????>              # JavaScript API
    YANDEX_GEOCODER_API_KEY=<???? ?????????>     # JavaScript API ? HTTP ????????
    REDIS_URL=redis://localhost:6379/0
    VIDEO_OUTPUT_DIR=videos
    LOG_DIR=logs

????? ?????????? ? ???????? ???????????? ???????. ??? ?????? ? ????????? ???????????? ?????? ???????, ??????? ????? ????? ??????????.

### 4. ??????

????? 3 ???????? (?????? ? ????????? ?????????):

1. Redis ? ???? ?????????? Memurai, ?????????, ??? ?????? ????????.

2. FastAPI (backend):

       .venv\Scripts\activate
       uvicorn backend.app.main:app --host 127.0.0.1 --port 8000 --reload

3. Celery worker:

       .venv\Scripts\activate
       celery -A backend.app.celery_app worker --loglevel=debug --pool=solo

4. (???????????) Celery beat:

       .venv\Scripts\activate
       celery -A backend.app.celery_app beat --loglevel=info

????? ????? ???????????? ?????? run_all.bat ??? ??????????????? ??????? ??? Windows.

### 5. ???-?????????

???????? ? ????????: http://127.0.0.1:8000

## ?????? ?? Ubuntu-???????

    sudo apt update
    sudo apt install -y python3 python3-venv python3-pip ffmpeg redis-server git

    git clone <URL_REPO> maps && cd maps
    python3 -m venv .venv && source .venv/bin/activate
    pip install -r backend/requirements.txt

    cp .env.example .env
    # ?????????????? .env

    sudo systemctl enable --now redis-server

    uvicorn backend.app.main:app --host 0.0.0.0 --port 5555
    celery -A backend.app.celery_app worker --loglevel=debug --concurrency=1
    celery -A backend.app.celery_app beat --loglevel=info

??? ???????? ? ??????????? ???? ??????? systemd-?????:

- mapvideo-uvicorn.service
- mapvideo-celery-worker.service
- mapvideo-celery-beat.service
- mapvideo.target (???? 5555; ??? ????? ????? ? ?????? --port ? unit ? ???????)

## ????????? ???????

    maps/
      backend/
        app/
          main.py            # FastAPI
          config.py          # ????????????
          celery_app.py      # Celery
          api/routes.py      # HTTP-?????????
          services/          # ????????, ?????, ?????????, FFmpeg
          tasks/             # Celery-??????
          models/schemas.py  # Pydantic-?????
        requirements.txt
      frontend/              # HTML/CSS/JS
      videos/                # ??????????????? ????? (gitignored)
      logs/                  # ???? (gitignored)
      .env.example
      SPECIFICATION.md
